#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        vector<string> v(n);
        string s;
        for (int i = 0; i < n; i++)
            cin >> v[i];
        cin >> s;
        for (int i = n - 1; i >= 0; i++)
        {
            
        }
    }
}